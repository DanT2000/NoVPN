<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Тему рисует Compose; системной нужен только фон окна без панели заголовка,
         иначе при запуске мелькает белый прямоугольник поверх тёмной темы. -->
    <style name="Theme.NoVPN" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
