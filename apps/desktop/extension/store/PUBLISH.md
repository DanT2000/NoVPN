# Публикация расширения NoVPN

Всё, что нужно для трёх магазинов: готовые архивы, тексты и порядок действий.
Собрано так, чтобы переписывать это второй раз не пришлось.

## Что уже готово

| Файл | Для чего |
|---|---|
| `dist/novpn-extension-chrome.zip` | Chrome Web Store |
| `dist/novpn-extension-edge.zip` | Microsoft Edge Add-ons |
| `dist/novpn-extension-firefox.zip` | Firefox Add-ons (AMO) |
| `screenshots/*.png` | Скриншоты 1280×800, годятся во все три магазина |

Пересобрать архивы: `python apps/desktop/scripts/package-extension.py`

Firefox собирается отдельно и не случайно: там событийная страница вместо service
worker, свой идентификатор `novpn@appswire.ru` (по нему приложение пускает расширение
к нативному хосту) и нет полей `key` и `minimum_chrome_version`. У Chrome и Edge поле
`key` **остаётся**: из него браузер выводит идентификатор расширения, и он одинаков при
установке из архива на любом компьютере и после публикации в магазине — именно этот
идентификатор зашит в приложении (`EXTENSION_ID` в `host.rs`). Без `key` каждая
распакованная установка получала бы случайный идентификатор, и приложение отказывало бы
расширению в доступе к нативному хосту (так и было до версии 0.2).

---

## Тексты для карточки

### Название

    NoVPN — маршрут для сайта

### Краткое описание (до 132 символов)

    Прямо на сайте: через VPN или напрямую, вместе с попутными доменами. Правило сразу попадает в приложение NoVPN.

### Полное описание

    Расширение для тех, кто пользуется приложением NoVPN на компьютере.

    Открыли сайт — нажали на значок — выбрали, как он ходит: через VPN или напрямую.
    Правило применяется за пару секунд, без переподключения и перезапуска.

    Зачем это нужно

    Умная маршрутизация ведёт большинство сайтов сама: что не работает в России — через
    VPN, остальное напрямую. Но списки не знают про ваши личные случаи. Российский банк,
    который не любит зарубежный адрес; рабочий сервис, доступный только из своей сети;
    сайт, который вчера работал, а сегодня нет. Раньше ради этого нужно было идти в
    приложение и добавлять домен руками. Теперь — одно нажатие на самой странице.

    Попутные домены

    Сайт редко живёт на одном домене: картинки на своём CDN, интерфейс ходит в API на
    другом адресе. Отправили сайт через VPN — а половина страницы всё равно не грузится.
    Поэтому после выбора маршрута расширение показывает, какие ещё домены подгружала эта
    вкладка, и предлагает отправить их тем же путём: вы снимаете лишнее и подтверждаете.

    Как это устроено

    Расширение не читает содержимое страниц и не видит, что вы на них делаете. Оно
    берёт адрес активной вкладки в момент, когда вы нажали на значок, и — для попутных
    доменов — только имена хостов, к которым обращалась эта вкладка; этот список живёт в
    памяти и стирается при переходе на другую страницу. Дальше передаёт домены и ваш
    выбор приложению NoVPN на этом же компьютере.

    Связь с приложением идёт через нативные сообщения браузера, а не через открытый порт
    на компьютере: к порту может подключиться любая программа, а к нативному хосту —
    только те расширения, которые перечислены в его манифесте.

    Никакие данные никуда не отправляются. Ни аналитики, ни сторонних серверов.

    Что нужно для работы

    Установленное приложение NoVPN для Windows: https://vpn.appswire.ru/download
    Без него расширению некуда передавать правила — оно об этом честно скажет.

### Категория

Chrome и Edge — «Инструменты» (Tools / Productivity). Firefox — «Privacy & Security».

---

## Обоснование разрешений

Магазины требуют объяснить каждое разрешение, и отказы чаще всего именно из-за этого.
Формулировки готовы — вставлять как есть.

| Разрешение | Что писать в форме |
|---|---|
| `activeTab` | Нужен домен активной вкладки, чтобы показать, для какого сайта задаётся правило. Доступ возникает только после нажатия на значок и только к адресу, не к содержимому. |
| `tabs` | Читаем адрес активной вкладки при открытии окна расширения, чтобы подставить домен. Содержимое вкладок не используется. |
| `nativeMessaging` | Единственный канал связи с приложением NoVPN на том же компьютере: расширение передаёт ему домен и выбранный маршрут. Сетевых запросов расширение не делает. |
| `storage` | Запоминаем последний выбор для домена, чтобы окно открывалось с уже отмеченным вариантом. Хранится локально. |
| `webRequest` | Только наблюдение (`onCompleted`), без блокировки и изменения запросов. Расширение запоминает, к каким доменам обращалась открытая вкладка, чтобы после выбора «через VPN» предложить отправить тем же путём и попутные домены сайта — его CDN и API, без которых страница не работает. Список живёт в памяти, никуда не передаётся и стирается при переходе на другую страницу или закрытии вкладки. |
| `host_permissions: <all_urls>` | Нужно браузеру, чтобы `webRequest` видел адреса запросов на любом сайте: заранее неизвестно, на каком сайте человек нажмёт значок. Используется только имя хоста; содержимое страниц, запросов и ответов не читается и не меняется. |

**Про `<all_urls>` в комментарии к проверке:** прямо написать, что разрешение нужно для
одной функции — «попутные домены» (как в ZeroOmega/SwitchyOmega): расширение видит только
имена хостов, к которым обращалась вкладка, ничего не блокирует, не читает и никуда не
отправляет; все решения принимает человек в окне расширения. Обычно это снимает половину
вопросов. В Firefox доступ ко всем сайтам по умолчанию выключен — см. раздел AMO ниже.

---

## Политика конфиденциальности

Требуется всем трём магазинам. Размещена по адресу
`https://vpn.appswire.ru/extension/privacy.html` (файл `apps/web/public/extension/privacy.html`,
на русском и английском; уезжает с деплоем панели) — эту ссылку указывать в карточке.
Текст ниже — та же политика, для копирования в форму, если магазин просит текстом.

    Политика конфиденциальности расширения NoVPN

    Расширение не собирает, не хранит и не передаёт третьим лицам никаких персональных
    данных.

    Что расширение использует:

    - Адрес (домен) активной вкладки — только в момент, когда вы нажали на значок
      расширения. Он нужен, чтобы показать, для какого сайта вы задаёте правило.
    - Имена доменов, к которым обращается открытая вкладка (например, CDN и API
      сайта) — чтобы после выбора маршрута предложить отправить тем же путём и
      попутные домены. Этот список хранится только в оперативной памяти расширения,
      не записывается на диск, никуда не передаётся и стирается при переходе на другую
      страницу или закрытии вкладки. Содержимое запросов и ответов не читается.
    - Ваш выбор маршрута для этих доменов — передаётся приложению NoVPN, установленному
      на том же компьютере, через механизм нативных сообщений браузера. Ничего не
      добавляется без вашего подтверждения.

    Чего расширение не делает:

    - не читает и не изменяет содержимое страниц, запросов и ответов;
    - не блокирует и не перенаправляет запросы — маршрутизацией занимается приложение;
    - не делает собственных сетевых запросов и не обращается к сторонним серверам;
    - не использует аналитику и рекламные сети;
    - не передаёт данные за пределы вашего компьютера.

    Правила маршрутизации хранятся в файле на вашем компьютере и удаляются вместе с
    приложением NoVPN.

    Вопросы: https://vpn.appswire.ru

---

## Порядок публикации

### Chrome Web Store

1. Открыть <https://chrome.google.com/webstore/devconsole>, войти под своим Google-аккаунтом.
2. Разовый взнос разработчика — 5 $ (один раз на аккаунт).
3. «Добавить элемент» → загрузить `novpn-extension-chrome.zip`.
4. Заполнить карточку текстами выше, приложить скриншоты из `screenshots/`.
5. Вкладка «Конфиденциальность»: ссылка на политику и обоснования разрешений из
   таблицы. На вопросы о продаже и передаче данных — «Нет».
6. Отправить на проверку. Обычно 1–3 рабочих дня, при первой публикации бывает дольше.

**Идентификатор не изменится.** В манифесте есть поле `key`, и магазин выведет из него
тот же идентификатор, что и при установке из архива — `fcigljflnglmclncmdjmbfppppaoeech`.
Он уже зашит в приложении (`EXTENSION_ID` в `apps/desktop/src-tauri/src/host.rs`), поэтому
выпускать новую версию приложения после публикации не нужно. После одобрения один раз
сверить идентификатор в карточке магазина с этим значением.

### Microsoft Edge Add-ons

1. <https://partner.microsoft.com/dashboard/microsoftedge> — регистрация бесплатная.
2. «Новое расширение» → загрузить `novpn-extension-edge.zip`.
3. Те же тексты и скриншоты.
4. В разделе «Доступность» указать русский язык и страны (или все).
5. Проверка обычно дольше, чем в Chrome — до недели.

### Firefox Add-ons (AMO)

1. <https://addons.mozilla.org/developers/> — бесплатно.
2. «Submit a New Add-on» → загрузить `novpn-extension-firefox.zip`.
3. Выбрать «On this site» (публично в каталоге).
4. Firefox проверяет исходный код: расширение не минифицировано и ничем не собирается,
   поэтому отдельный архив с исходниками не нужен — так и указать в комментарии.
5. Идентификатор зафиксирован в манифесте (`novpn@appswire.ru`), после публикации
   менять приложение не придётся.
6. В Firefox (Manifest V3) доступ ко всем сайтам после установки **выключен**: человеку
   нужно открыть `about:addons` → NoVPN → вкладка «Разрешения» → включить «Доступ к вашим
   данным для всех сайтов». Без этого расширение работает, но попутные домены не
   предлагает. Написать это в описании карточки для AMO и в подсказке на сайте.

---

## English listing

### Name

    NoVPN — per-site routing

### Summary

    Route a site through the VPN or directly, right on the page, with its related domains. The rule reaches the NoVPN app instantly.

### Description

    A companion extension for the NoVPN desktop app.

    Open a site, click the icon, choose how it connects: through the VPN or directly. The
    rule applies within seconds — no reconnect, no restart.

    Smart routing already handles most sites on its own. But no list knows your personal
    cases: a bank that dislikes foreign addresses, a work service reachable only from your
    own network, a site that worked yesterday and does not today. Instead of opening the
    app and typing the domain, you decide right on the page.

    Related domains

    A site rarely lives on a single domain: images come from its CDN, the interface talks
    to an API elsewhere. Route the site through the VPN and half the page still fails to
    load. So after you pick a route, the extension shows which other domains this tab
    loaded and offers to send them the same way: untick what you don't need and confirm.

    Privacy

    The extension cannot read page content. It reads the address of the active tab only
    when you click its icon, and passes the domain plus your choice to the NoVPN app on
    the same computer.

    To suggest "related domains" (a site's CDN and API hosts that must take the same
    route), it observes the host names of requests made by the open tab. This list lives
    in memory only, is never written to disk or sent anywhere, and is discarded when the
    tab navigates or closes. Nothing is added to your rules without your confirmation.

    It talks to the app through the browser's native messaging, not through a local HTTP
    port: any program can reach a port, while a native host only accepts the extensions
    listed in its manifest.

    Nothing is sent anywhere. No analytics, no third-party servers.

    Requires the NoVPN app for Windows: https://vpn.appswire.ru/download

---

## Скриншоты

Три штуки, 1280×800, сняты с настоящего окна расширения — не макеты:

| Файл | О чём |
|---|---|
| `01-choose-route.png` | Выбор маршрута для сайта |
| `02-direct.png` | Российский сайт идёт напрямую |
| `03-rules-live.png` | Правило уже применено |

Пересобрать после изменений в окне расширения — скриптом `capture-ext.mjs`
(нужен Playwright; лежит рядом со скриптами скриншотов панели).
